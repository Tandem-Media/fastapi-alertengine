[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn fastapi_alertengine.app_main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health/alerts"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
