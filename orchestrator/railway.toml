[build]
builder = "nixpacks"

[deploy]
startCommand = "cd orchestrator && python main.py"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
